# machash - Hash arbitrary strings in to MAC addresses

This tool will hash input strings into fake, but valid, MAC addresses. It can optionally be used as a general tool for generating 48-bit hashes.
